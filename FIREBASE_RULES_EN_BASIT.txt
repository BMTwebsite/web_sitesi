⚠️ EN BASIT ÇÖZÜM - KOPYALA YAPIŞTIR ⚠️

Firebase Console > Firestore Database > Rules sekmesine gidin
TÜM MEVCUT KURALLARI SİLİN (Ctrl+A, Delete)
Aşağıdaki kuralları YAPIŞTIRIN:

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /pending_admins/{document=**} {
      allow write: if true;
      allow read: if true;
    }
    match /events/{document=**} {
      allow read: if true;
      allow write: if true;
    }
    match /admins/{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}

✅ Publish butonuna tıklayın
⏳ 30 saniye bekleyin
🔄 Uygulamayı yeniden başlatın (R tuşu)

⚠️ NOT: Bu kurallar geçici olarak HERKESE izin verir. 
   Production için daha güvenli kurallar kullanmalısınız.

